name: CI Pipeline with Chaos Test

on:
  push:
    branches: [develop]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Run Unit Tests
        run: npm test

      - name: Deploy to Staging
        run: ./deploy.sh staging
