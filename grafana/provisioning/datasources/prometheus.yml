apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: fixme-prometheus          # fixed UID so dashboard JSON can reference it reliably
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "5s"           # match Prometheus scrape_interval
